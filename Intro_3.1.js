/*2. javascript basic -- Is a Programming language Used to Design More Dynamically and Interative --
Js can Do --
a.Handle button click events
b.Validate form inputs
c.Validate form inputs
d.Change website content dynamically
e.Running Enviroment is Node.js

Here example of Printing-- 
*/
console.log("Hello Champ")

// 02. Null vs Undefined & typeof --
let b = null;

//b. Undefined 
let a;
console.log(a);

// c.typeof Operator
typeof 10;        // "number"
typeof true;      // "boolean"
typeof "Hello";   // "string"
typeof null;      // "object" (this is a known JavaScript bug)
typeof undefined; // "undefined"

// 03. Operators (+ - * % / **) example's
10 + 5; // 15
10*10; // 100
5-3;  // 2
10%10 // 0 etc..

// Comparison Operators (== != === !== < >)
10 == "10";   // true   
7 === "7";  // false (Assign value and data type)

// Logical Operators (&& (AND),   || (OR),  ! (NOT))..
true && false; // false
true || false; // True
console.log(!6 == 4); // it Working Opposite to Our Condition..

// 4. Type Conversion -- Also See convert Number or Str
Number("5");  // 10
String(10);    // "10"
Boolean(2);    // trur
Number("10");   // 10
parseInt("20"); // 20
Number("10");   // 10
parseInt("20"); // 20

//  Veriable and DataType  dec.. 3 Type in js 
let name = "warner"; // js Allowed to changebal 
name = "Martin Guptil" // yes Done 
 const age = 25; // Not Change 







 


 
 
